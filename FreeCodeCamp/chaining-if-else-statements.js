function testSize(num) {
  // Only change code below this line
  //if(num){}else if(num){}else if(num){}else if(num){}else

  return num < 5 ? "Tiny"
:num < 10 ? "Small"
:num < 15 ? "Medium"
:num < 20 ? "Large"
:num >= 20 ?"Huge"
:"Change Me";
  
  if(num){}else if(num){}else if(num){}else if(num){}else {}

  // Only change code above this line
}

testSize(7);